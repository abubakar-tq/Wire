{
  "name": "@wirefluid/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./abis": "./src/abis.ts",
    "./addresses": "./src/addresses.ts",
    "./chain": "./src/chain.ts",
    "./roles": "./src/roles.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url https://evm.wirefluid.com --account burner --sender $SENDER_ADDRESS --broadcast --legacy"
  },
  "dependencies": {
    "viem": "^2.31.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
